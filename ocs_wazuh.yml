---
- hosts: all
  become: true
  gather_facts: false

  vars:
    wazuh_version_objetivo: "4.14.7-1"
    wazuh_deb_url: "https://packages.wazuh.com/4.x/apt/pool/main/w/wazuh-agent/wazuh-agent_4.14.7-1_amd64.deb"

    ocs_url: "https://inventariolinux.presidencia.gob.ar/ocsinventory"

    ocs_cert_dir: "/var/lib/ocsinventory-agent/https:__inventariolinux.presidencia.gob.ar_ocsinventory"

  tasks:

##############################################################################
# INFORMACION DEL HOST
##############################################################################

    - name: Mostrar host
      debug:
        msg: "Procesando {{ inventory_hostname }}"

##############################################################################
# APT PROXY
##############################################################################

    - name: Verificar si existe 00aptproxy
      stat:
        path: /etc/apt/apt.conf.d/00aptproxy
      register: aptproxy

    - name: Comentar proxy APT antiguo
      replace:
        path: /etc/apt/apt.conf.d/00aptproxy
        regexp: '^Acquire::http::Proxy "http://apt-cache\.presidencia\.local:3142";'
        replace: '# Acquire::http::Proxy "http://apt-cache.presidencia.local:3142";'
      when: aptproxy.stat.exists

##############################################################################
# MANEJO INTELIGENTE DE APT BLOQUEADO
##############################################################################

- name: Detener unattended-upgrades
  service:
    name: unattended-upgrades
    state: stopped
  failed_when: false

- name: Esperar 60 segundos a que apt libere locks
  shell: |
    timeout=60

    while [ $timeout -gt 0 ]; do
      if ! fuser /var/lib/dpkg/lock >/dev/null 2>&1 && \
         ! fuser /var/lib/dpkg/lock-frontend >/dev/null 2>&1 && \
         ! fuser /var/lib/apt/lists/lock >/dev/null 2>&1 && \
         ! fuser /var/cache/apt/archives/lock >/dev/null 2>&1; then
         exit 0
      fi

      sleep 5
      timeout=$((timeout-5))
    done

    exit 1
  register: apt_wait
  failed_when: false
  changed_when: false

- name: Buscar proceso que mantiene lock
  shell: |
    (
      fuser /var/lib/dpkg/lock 2>/dev/null
      fuser /var/lib/dpkg/lock-frontend 2>/dev/null
      fuser /var/lib/apt/lists/lock 2>/dev/null
      fuser /var/cache/apt/archives/lock 2>/dev/null
    ) | tr ' ' '\n' | sort -u | grep -E '^[0-9]+$' || true
  register: apt_lock_pids
  changed_when: false
  when: apt_wait.rc != 0

- name: Mostrar PID bloqueante
  debug:
    msg: "PID bloqueando APT: {{ apt_lock_pids.stdout_lines }}"
  when: apt_wait.rc != 0

- name: Finalizar proceso bloqueante
  shell: |
    for pid in {{ apt_lock_pids.stdout_lines | join(' ') }}; do
      kill -9 "$pid" || true
    done
  when:
    - apt_wait.rc != 0
    - apt_lock_pids.stdout != ""
  failed_when: false

- name: Eliminar locks residuales
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - /var/lib/dpkg/lock
    - /var/lib/dpkg/lock-frontend
    - /var/lib/apt/lists/lock
    - /var/cache/apt/archives/lock

- name: Reconfigurar paquetes pendientes
  command: dpkg --configure -a
  failed_when: false

- name: Reparar dependencias
  command: apt-get -f install -y
  failed_when: false

##############################################################################
# OCS INVENTORY
##############################################################################

    - name: Verificar version instalada de OCS
      shell: |
        dpkg-query -W -f='${Version}' ocsinventory-agent 2>/dev/null || echo NOINSTALADO
      register: ocs_version
      changed_when: false

    - name: Mostrar version OCS
      debug:
        msg: "Version OCS: {{ ocs_version.stdout }}"

    - name: Instalar o actualizar OCS
      apt:
        name: ocsinventory-agent
        state: latest
        update_cache: yes

    - name: Verificar archivo de configuracion OCS
      stat:
        path: /etc/ocsinventory/ocsinventory-agent.cfg
      register: ocs_cfg

    - name: Corregir URL HTTP por HTTPS
      replace:
        path: /etc/ocsinventory/ocsinventory-agent.cfg
        regexp: '^server=http://inventariolinux\.presidencia\.gob\.ar/ocsinventory'
        replace: 'server=https://inventariolinux.presidencia.gob.ar/ocsinventory'
      when: ocs_cfg.stat.exists

    - name: Corregir configuracion local incorrecta
      replace:
        path: /etc/ocsinventory/ocsinventory-agent.cfg
        regexp: '^local=/var/lib/ocsinventory-agent/reports'
        replace: 'server=https://inventariolinux.presidencia.gob.ar/ocsinventory'
      when: ocs_cfg.stat.exists

    - name: Crear directorio SSL OCS
      file:
        path: "{{ ocs_cert_dir }}"
        state: directory
        mode: "0755"

    - name: Eliminar certificado viejo
      file:
        path: "{{ ocs_cert_dir }}/cacert.pem"
        state: absent

    - name: Descargar certificado OCS
      shell: |
        openssl s_client \
          -connect inventariolinux.presidencia.gob.ar:443 \
          -servername inventariolinux.presidencia.gob.ar \
          -showcerts </dev/null 2>/dev/null \
          | awk '/BEGIN CERTIFICATE/,/END CERTIFICATE/' \
          > {{ ocs_cert_dir }}/cacert.pem
      args:
        executable: /bin/bash

    - name: Validar certificado descargado
      command: >
        openssl x509
        -in {{ ocs_cert_dir }}/cacert.pem
        -noout
      register: cert_check
      failed_when: false
      changed_when: false

    - name: Mostrar validacion certificado
      debug:
        var: cert_check.rc

    - name: Mostrar configuracion final OCS
      shell: |
        grep -E 'server=|local=' /etc/ocsinventory/ocsinventory-agent.cfg
      register: ocs_cfg_final
      failed_when: false
      changed_when: false

    - debug:
        var: ocs_cfg_final.stdout_lines

    - name: Ejecutar inventario OCS
      command: ocsinventory-agent --debug
      register: ocs_result
      failed_when: false

##############################################################################
# WAZUH
##############################################################################

    - name: Detectar version instalada de Wazuh
      shell: |
        dpkg-query -W -f='${Version}' wazuh-agent 2>/dev/null || echo NOINSTALADO
      register: wazuh_version
      changed_when: false

    - name: Mostrar version Wazuh
      debug:
        msg: "Version Wazuh: {{ wazuh_version.stdout }}"

    - name: Descargar Wazuh 4.14.7
      get_url:
        url: "{{ wazuh_deb_url }}"
        dest: /tmp/wazuh-agent_4.14.7-1_amd64.deb
      when: >
        wazuh_version.stdout == "NOINSTALADO" or
        wazuh_version_objetivo not in wazuh_version.stdout

    - name: Instalar o actualizar Wazuh
      shell: |
        WAZUH_MANAGER='172.21.205.100' \
        dpkg -i /tmp/wazuh-agent_4.14.7-1_amd64.deb
      when: >
        wazuh_version.stdout == "NOINSTALADO" or
        wazuh_version_objetivo not in wazuh_version.stdout

    - name: Recargar systemd
      command: systemctl daemon-reload
      changed_when: false

    - name: Habilitar Wazuh
      systemd:
        name: wazuh-agent
        enabled: true

    - name: Iniciar Wazuh
      systemd:
        name: wazuh-agent
        state: started

    - name: Verificar estado Wazuh
      command: systemctl is-active wazuh-agent
      register: wazuh_estado
      changed_when: false
      failed_when: false

##############################################################################
# RESUMEN FINAL
##############################################################################

    - name: Mostrar resumen
      debug:
        msg:
          - "OCS: {{ ocs_version.stdout }}"
          - "Wazuh: {{ wazuh_version.stdout }}"
          - "Estado Wazuh: {{ wazuh_estado.stdout }}"
